<!-- navbar -->
<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container">
        <a class="navbar-brand" href="/index">
            <img src="/assets/img/—Pngtree—coffee_3626459.png" alt="Bootstrap" width="50" />
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link {{#if (equal currentPage 'Home')}} active fw-bold{{/if}}" aria-current="page"
                        href="/index">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if (equal currentPage 'Blog')}} active fw-bold{{/if}}" aria-current="page"
                        href="/blog">Blog</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if (equal currentPage 'My Project')}} active fw-bold{{/if}}"
                        aria-current="page" href="/project">My Project</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if (equal currentPage 'Testimonials')}} active fw-bold{{/if}}"
                        aria-current="page" href="/testimonials">Testimonials</a>
                </li>
            </ul>

            <div>
                {{#if user}}
                <span class="me-3">Selamat datang, {{user.name}}</span>
                <a href="/logout" class="me-3">Logout</a>
                {{/if}}
                {{#unless user}}
                <a class="btn" class="nav-link" href="login">Login</a>
                {{/unless}}
                <a class="nav-contact-button" class="nav-link" href="/contact">Contact me</a>
            </div>
        </div>
    </div>
</nav>